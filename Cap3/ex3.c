/* 3) Faça um programa que leia três valores inteiros e mostre sua
soma. */

#include <stdio.h>

int main() {
    int b, n, m;
    b = 48;
    n = 64;
    m = 16;
    printf("%d + %d + %d = %d", b, n, m, b + n + m);
    return 0;
}